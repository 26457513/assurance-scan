{
  "name": "advise-remediation",
  "description": "Pull the latest detailed findings for a repository and recommend prioritised approaches to address the critical/high issues.",
  "parameters": [
    {
      "name": "project",
      "description": "Repository or project identity: org/repo, github:org/repo, or a local path.",
      "default": ""
    }
  ],
  "prompt": "Use the `assurance-scan` MCP server. Goal: recommend approaches to address the critical issues in {{project}}.\n\n1. Call `get_project_findings` with project=\"{{project}}\" and severity=\"CRITICAL\". If empty, retry with severity=\"HIGH\".\n2. Cluster the findings by rule_id and theme \u2014 a cluster shares one root cause and one fix.\n3. For each cluster, recommend an approach. Use the finding's `fix_strategy` when present; prefer config changes and dependency bumps over code rewrites when they fix the same rule.\n4. Order clusters by severity, then finding count, then whether any finding carries compliance_tags.\n5. Present a table: cluster | findings | affected files | recommended approach | effort (S/M/L).\n6. Offer to act: `code-finding-fix` for config-level fixes, `code-fr-test` when a test proves the fix, `scan-project` to re-scan and verify.\n\nEvery row must cite rule_id and file paths taken from the payload \u2014 do not invent findings."
}
