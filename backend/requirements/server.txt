# Server-side Python dependencies for the assurance-scan service.
# Pinned for reproducibility. Update via `pip install --upgrade` + re-pin.

fastapi>=0.128,<0.130
uvicorn[standard]>=0.34,<0.36
pydantic>=2.11,<3
SQLAlchemy[asyncio]>=2.0,<2.1
aiosqlite>=0.20,<0.21
alembic>=1.14,<1.16
jsonschema>=4.23,<5.0
mcp>=1.28.1,<2.0
python-multipart>=0.0.30
msgpack>=1.2.1
defusedxml>=0.7,<1.0
