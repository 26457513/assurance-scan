# Tapestry Security Assurance Scanner — environment file
#
# Copy to .env and fill in. The run-local.sh entrypoint sources this file
# before reading scanner-config.yaml, so yaml values override .env when both
# set the same key. Environment variables exported in your shell override both.
#
# v4 no longer requires an NVD API key — dependency-check was replaced by
# osv-scanner, which uses OSV.dev (no key, no rate limit). These keys are all
# optional and only affect scanner behavior in specific scenarios.

# Optional tokens for scanner ecosystems. Most local scans leave these blank.
GITHUB_TOKEN=
TRIVY_TOKEN=
SEMGREP_APP_TOKEN=
ZAP_API_KEY=

# Fallback per-scanner timeout (seconds).
SCANNER_TIMEOUT_DEFAULT=600

# GitHub CI polling (phase 2 — server ingests completed assurance-scan runs).
# Fine-grained PAT, resource owner = the org, Actions:Read + Contents:Read.
GITHUB_POLL_TOKEN=
# Org whose repos are listed/polled (overrides the repo list below).
GITHUB_ORG=26457513
# Optional manual repo list override (comma-separated); empty uses the org.
POLL_REPOS=
# Poll cadence in seconds (default 60).
POLL_INTERVAL_SECONDS=60

# Shared Basic Auth for hosted deployments (unset both = auth disabled).
APP_AUTH_USER=
APP_AUTH_PASSWORD=

# Google Workspace login (replaces the Basic Auth prompt for browsers when
# all four are set; Basic Auth stays valid as an API fallback).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_DOMAIN=barkleygen.com
SESSION_SECRET=<random 32+ char string>
PUBLIC_BASE_URL=https://scan.yourdomain.com
