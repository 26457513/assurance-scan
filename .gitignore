# Local secrets — never commit
.env
scanner-config.yaml

# Scanner output
reports/
.assurance-scan/

# Local proposal/output scratch files
proposal*.json
proposal*.md
*.fr-catalog.draft.json
*.fr-catalog.enriched.json
*.assurance-framework.draft.json

# Per-project scannerignore (copy .scannerignore.example into the target repo)
.scannerignore

# Python bytecode caches
__pycache__/
*.pyc
data/authority-sources/raw/

# Config files now stored in DB — don't commit local copies
fr-catalog.json
fr-compliance-mapping.json
