name: publish-app-image

on:
  push:
    branches: [main]
    paths:
      - Dockerfile
      - frontend/**
      - backend/app/**
      - backend/resources/**
      - backend/scripts/**
      - backend/bin/**
      - backend/requirements/server.txt
      - backend/alembic.ini
      - docker-compose.security.yml
      - compose.yaml
      - .github/workflows/publish-app-image.yml

permissions:
  contents: read

env:
  IMAGE: ghcr.io/26457513/assurance-scan-app

concurrency:
  group: publish-app-image
  cancel-in-progress: false

jobs:
  build-candidate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3
      - uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build, attest, and push immutable application candidate
        id: build
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ env.IMAGE }}:sha-${{ github.sha }}
          build-args: |
            VERSION=sha-${{ github.sha }}
            REVISION=${{ github.sha }}
          sbom: true
          provenance: mode=max
      - uses: sigstore/cosign-installer@d7543c93d881b35a8faa02e8e3605f69b7a1ce62 # v3.10.0
      - name: Keyless-sign the immutable application digest
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
        run: cosign sign --yes "$IMAGE@$DIGEST"
      - name: Verify immutable digest, signature, SBOM, and provenance
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          test "$(docker buildx imagetools inspect "$IMAGE:sha-$GITHUB_SHA" --format '{{.Manifest.Digest}}')" = "$DIGEST"
          test "$(docker buildx imagetools inspect "$IMAGE@$DIGEST" --format '{{.Manifest.Digest}}')" = "$DIGEST"
          docker buildx imagetools inspect "$IMAGE@$DIGEST" --format '{{json .Manifest}}' > manifest.json
          python -c 'import json; data=json.load(open("manifest.json")); matches=[item for item in data.get("manifests", []) if item.get("platform", {}).get("os") == "linux" and item.get("platform", {}).get("architecture") == "amd64"]; assert len(matches) == 1, "release must contain exactly one linux/amd64 manifest"; print(matches[0]["digest"])' > platform-digest.txt
          cosign verify "$IMAGE@$DIGEST" \
            --certificate-identity 'https://github.com/26457513/assurance-scan/.github/workflows/publish-app-image.yml@refs/heads/main' \
            --certificate-oidc-issuer https://token.actions.githubusercontent.com
          docker buildx imagetools inspect "$IMAGE@$DIGEST" \
            --format '{{ json .SBOM.SPDX }}' > sbom.json
          docker buildx imagetools inspect "$IMAGE@$DIGEST" \
            --format '{{ json .Provenance.SLSA }}' > provenance.json
          python - "$GITHUB_SHA" "https://github.com/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID/attempts/$GITHUB_RUN_ATTEMPT" <<'PY'
          import json
          import sys

          expected_sha, expected_builder = sys.argv[1:]
          with open("sbom.json", encoding="utf-8") as stream:
              sbom = json.load(stream)
          assert sbom.get("SPDXID") == "SPDXRef-DOCUMENT", "invalid SPDX SBOM"
          assert sbom.get("packages"), "SPDX SBOM contains no packages"

          with open("provenance.json", encoding="utf-8") as stream:
              provenance = json.load(stream)
          definition = provenance.get("buildDefinition", {})
          request_args = definition.get("externalParameters", {}).get("request", {}).get("args", {})
          builder = provenance.get("runDetails", {}).get("builder", {}).get("id")
          assert definition.get("buildType"), "SLSA provenance has no build type"
          assert request_args.get("build-arg:REVISION") == expected_sha, "provenance revision mismatch"
          assert request_args.get("build-arg:VERSION") == f"sha-{expected_sha}", "provenance version mismatch"
          assert builder == expected_builder, "provenance builder mismatch"
          PY
          platform_digest=$(cat platform-digest.txt)
          docker run --rm --platform linux/amd64 "$IMAGE@$platform_digest" --help | grep -q 'Assurance Scan'
      - uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Move candidate to the verified digest without rebuilding
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          docker buildx imagetools create --tag "$IMAGE:candidate" "$IMAGE@$DIGEST"
          test "$(docker buildx imagetools inspect "$IMAGE:candidate" --format '{{.Manifest.Digest}}')" = "$DIGEST"
      - name: Record immutable application candidate
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          {
            printf '### Application candidate\n\n'
            printf -- '- Revision: `%s`\n' "$GITHUB_SHA"
            printf -- '- Immutable image: `%s@%s`\n' "$IMAGE" "$DIGEST"
            printf -- '- Registry retrieval, signature, SBOM, provenance, and candidate equality: passed\n'
            printf -- '- No production tag was changed and no deployment was performed.\n'
          } >> "$GITHUB_STEP_SUMMARY"
